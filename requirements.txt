requests>=2.31
python-dotenv>=1.0

# Produzione contenuti (agents/): script + voce
# Serve anche ffmpeg/ffprobe a livello di sistema: `brew install ffmpeg`
anthropic>=0.40
elevenlabs>=1.0

# Caroselli Instagram (agents/carousel_*): rendering slide HTML/CSS → PNG
# Dopo l'installazione serve anche: playwright install chromium
jinja2>=3.1
playwright>=1.45
# Immagine di copertina (agents/cover_image.py): stilizzazione con filtri
Pillow>=10.0
numpy>=1.24

# X (Twitter) — necessario quando attivi l'adapter twitter
requests-oauthlib>=1.3

# YouTube — installa solo quando attivi l'adapter youtube:
#   pip install google-api-python-client google-auth google-auth-oauthlib
